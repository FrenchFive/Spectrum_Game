name = "spectrum-suggest"
main = "spectrum-suggest.js"
compatibility_date = "2024-09-01"

# Deploy:        npx wrangler deploy
# Add the token: npx wrangler secret put GITHUB_TOKEN
# (optional) set ALLOW_ORIGIN to your game's origin instead of "*":
# [vars]
# ALLOW_ORIGIN = "https://frenchfive.github.io"
